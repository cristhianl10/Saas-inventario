# 🔧 Solución al Error de PostgreSQL

## ❌ Errores Actuales

### Error 1: Al crear/editar categorías
```
PostgrestException(message: Could not find the 'descripcion' column of 'categorias' 
in the schema cache, code: PGRST204, details: Bad Request, hint: null)
```

### Error 2: Al crear/editar productos
```
PostgrestException(message: Could not find the 'fecha_venta' column of 'productos' 
in the schema cache, code: PGRST204, details: Bad Request, hint: null)
```

## 🎯 Causa del Problema

Tu base de datos tiene columnas faltantes:

**Tabla `categorias`** le falta:
- `descripcion` (TEXT)

**Tabla `productos`** le faltan:
- `vendido` (BOOLEAN)
- `fecha_venta` (TIMESTAMP)
- `vendido_a` (TEXT)
- `precio_venta` (DECIMAL)

## ✅ Solución Rápida (3 pasos)

### Paso 1: Ir a Supabase SQL Editor

1. Abre tu proyecto en [Supabase](https://supabase.com)
2. Ve al menú lateral izquierdo
3. Haz clic en **SQL Editor** (ícono de base de datos)
4. Haz clic en **New query** (Nueva consulta)

### Paso 2: Ejecutar el Script de Corrección

Copia y pega este código en el editor SQL:

```sql
-- Agregar columna descripcion a categorias
ALTER TABLE categorias 
ADD COLUMN IF NOT EXISTS descripcion TEXT;

-- Agregar columnas faltantes a productos
ALTER TABLE productos 
ADD COLUMN IF NOT EXISTS vendido BOOLEAN DEFAULT FALSE,
ADD COLUMN IF NOT EXISTS fecha_venta TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS vendido_a TEXT,
ADD COLUMN IF NOT EXISTS precio_venta DECIMAL(10,2);
```

Luego haz clic en **Run** (Ejecutar) o presiona `Ctrl + Enter`

### Paso 3: Verificar que Funcionó

Ejecuta esta consulta para verificar:

```sql
-- Ver columnas de categorias
SELECT column_name, data_type 
FROM information_schema.columns
WHERE table_name = 'categorias'
ORDER BY ordinal_position;

-- Ver columnas de productos
SELECT column_name, data_type 
FROM information_schema.columns
WHERE table_name = 'productos'
ORDER BY ordinal_position;
```

**Tabla categorias** debe tener:
- id
- nombre
- emoji
- color
- **descripcion** ← Nueva

**Tabla productos** debe tener:
- id
- categoria_id
- nombre
- descripcion
- cantidad
- precio
- fecha_actualizacion
- **vendido** ← Nueva
- **fecha_venta** ← Nueva
- **vendido_a** ← Nueva
- **precio_venta** ← Nueva

---

## 🔄 Script Completo de Base de Datos (Alternativa)

Si prefieres recrear toda la base de datos desde cero:

```sql
-- ADVERTENCIA: Esto eliminará todos los datos existentes
-- Solo usa esto si no tienes datos importantes

-- Eliminar tablas existentes
DROP TABLE IF EXISTS ventas CASCADE;
DROP TABLE IF EXISTS productos CASCADE;
DROP TABLE IF EXISTS categorias CASCADE;

-- Crear tabla categorias
CREATE TABLE categorias (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nombre TEXT NOT NULL,
    emoji TEXT DEFAULT '📦',
    color TEXT DEFAULT '#ffffff',
    descripcion TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Crear tabla productos
CREATE TABLE productos (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    categoria_id BIGINT REFERENCES categorias(id) ON DELETE CASCADE,
    nombre TEXT NOT NULL,
    descripcion TEXT,
    cantidad INTEGER DEFAULT 0,
    precio DECIMAL(10,2),
    fecha_actualizacion TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    vendido BOOLEAN DEFAULT FALSE,
    fecha_venta TIMESTAMP WITH TIME ZONE,
    vendido_a TEXT,
    precio_venta DECIMAL(10,2)
);

-- Crear tabla ventas
CREATE TABLE ventas (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    producto_id BIGINT REFERENCES productos(id) ON DELETE CASCADE,
    cantidad INTEGER NOT NULL,
    precio_unitario DECIMAL(10,2) NOT NULL,
    total DECIMAL(10,2) NOT NULL,
    fecha_venta TIMESTAMP WITH TIME ZONE NOT NULL,
    vendido_a TEXT,
    observaciones TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Crear índices para mejor rendimiento
CREATE INDEX idx_productos_categoria ON productos(categoria_id);
CREATE INDEX idx_productos_vendido ON productos(vendido);
CREATE INDEX idx_ventas_producto ON ventas(producto_id);
CREATE INDEX idx_ventas_fecha ON ventas(fecha_venta);

-- Habilitar Row Level Security (RLS)
ALTER TABLE categorias ENABLE ROW LEVEL SECURITY;
ALTER TABLE productos ENABLE ROW LEVEL SECURITY;
ALTER TABLE ventas ENABLE ROW LEVEL SECURITY;

-- Políticas de acceso (permitir todo por ahora)
CREATE POLICY "Permitir todo en categorias" ON categorias FOR ALL USING (true);
CREATE POLICY "Permitir todo en productos" ON productos FOR ALL USING (true);
CREATE POLICY "Permitir todo en ventas" ON ventas FOR ALL USING (true);
```

---

## 🧪 Probar la Solución

Después de ejecutar el script:

1. **Cierra la app** completamente (no solo minimizar)
2. **Abre la app** nuevamente
3. **Intenta crear una categoría** → Debería funcionar ✅
4. **Intenta editar una categoría** → Debería funcionar ✅
5. **Intenta crear un producto** → Debería funcionar ✅
6. **Intenta editar un producto** → Debería funcionar ✅

---

## 📋 Resumen de Columnas por Tabla

### Tabla: categorias
- id (BIGINT, PK)
- nombre (TEXT)
- emoji (TEXT)
- color (TEXT)
- **descripcion (TEXT)** ← Necesaria
- created_at (TIMESTAMP) - opcional

### Tabla: productos
- id (BIGINT, PK)
- categoria_id (BIGINT, FK)
- nombre (TEXT)
- descripcion (TEXT)
- cantidad (INTEGER)
- precio (DECIMAL)
- fecha_actualizacion (TIMESTAMP)
- **vendido (BOOLEAN)** ← Necesaria
- **fecha_venta (TIMESTAMP)** ← Necesaria
- **vendido_a (TEXT)** ← Necesaria
- **precio_venta (DECIMAL)** ← Necesaria

### Tabla: ventas
- id (BIGINT, PK)
- producto_id (BIGINT, FK)
- cantidad (INTEGER)
- precio_unitario (DECIMAL)
- total (DECIMAL)
- fecha_venta (TIMESTAMP)
- vendido_a (TEXT)
- observaciones (TEXT)
- created_at (TIMESTAMP)

---

## ⚠️ Notas Importantes

1. **No perderás datos**: El comando `ALTER TABLE ADD COLUMN IF NOT EXISTS` solo agrega las columnas faltantes sin borrar nada.

2. **Valores por defecto**: Las nuevas columnas tendrán estos valores:
   - `vendido`: FALSE (no vendido)
   - `fecha_venta`: NULL (sin fecha)
   - `vendido_a`: NULL (sin cliente)
   - `precio_venta`: NULL (sin precio)

3. **Productos existentes**: Todos tus productos actuales se marcarán automáticamente como "no vendidos".

---

## 🆘 Si Sigues Teniendo Problemas

1. **Verifica la conexión a Supabase**:
   - URL correcta en `main.dart`
   - API Key correcta

2. **Revisa los permisos RLS**:
   - Ve a Authentication → Policies
   - Asegúrate de tener políticas que permitan INSERT/UPDATE

3. **Limpia la caché de la app**:
   ```bash
   cd inventario_app
   flutter clean
   flutter pub get
   flutter run
   ```

4. **Revisa los logs de Supabase**:
   - Ve a Logs en el panel de Supabase
   - Busca errores específicos

---

## ✅ Después de Corregir

Una vez que ejecutes el script SQL, tu app debería:
- ✅ Crear categorías sin errores
- ✅ Crear productos sin errores
- ✅ Editar productos sin errores
- ✅ Actualizar cantidades sin errores
- ✅ Registrar ventas correctamente

¡Tu base de datos estará lista para usar! 🚀
